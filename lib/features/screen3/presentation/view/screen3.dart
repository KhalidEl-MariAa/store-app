import 'package:flutter/material.dart';

class MyWidget extends StatelessWidget {
  const MyWidget({super.key});
  static String Sroute='mywidget';

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

